services:
  trimiata_1c_bot:
    container_name: trimiata_1c_bot
    build:
      context: ../
      dockerfile: src/Dockerfile
    env_file:
      - .env
    volumes:
      - ${PHOTO_SOURCES_PATH}:${PHOTO_SOURCES_PATH}